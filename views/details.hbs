{{#with cube}}
      <h1>{{name}}</h1>
      <img class="cube" src="{{imageUrl}}">
      <div class="details">
        <p><span>Creator:</span> {{creator}}</p>
        <p><span>Description:</span> {{description}}</p>
        <p><span>Difficulty level:</span> {{difficultyLevel}}</p>
        <a class="btn" href="/products">Back</a>
        {{#if isOwner}}
        <a class="btn" href="/products/details/{{_id}}/attach">Attach Stickers</a>
        <a class="btn" href="/products/edit/{{_id}}">Edit</a>
        <a class="btn" href="/products/delete/{{_id}}">Delete</a>
        {{/if}}
      </div>
        <h2>Accessories</h2>
            <div class="accessories">
               {{#each accessories}}
               {{> accessory}}
               {{else}}
               
                <h3 class="italic">This cube has no accessories yet...</h3>
                {{/each}}
            </div>
      <div class="details">
        <form action="/comments/{{_id}}/create" method="POST">
        <span>Comment: </span><textarea name="content" ></textarea><br>
        <input type="submit" value="Post Comment">
        </form>
        {{#each comments}}
        {{> comment}}
        {{else}}
        <span style="font-style:italic;">No comment yet. Write the first!</span>
        {{/each}}
      </div>
  {{/with}}
   